export const QUICK_PLANNER_COLORS = Object.freeze({
  onColor: "#FFFFFF",
  active: "#007AFF",
});

export const QUICK_PLANNER_METRICS = Object.freeze({
  dockHeight: 80,
  dockRadius: 40,
  closeTargetSize: 44,
  closeVisualSize: 36,
  closeVisualGap: 8,
  badgeSize: 30,
  badgeRingSize: 36,
  tileHeight: 72,
});
